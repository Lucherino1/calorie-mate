export enum EElComponentType {
  primary = 'primary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
  info = 'info'
}

export enum EElComponentSize {
  small = 'small',
  default = 'default',
  large = 'large'
}

export enum ELayouts {
  default = 'default',
  blank = 'blank'
}

export enum ERoles {
  user = 'user',
  admin = 'admin',
}
